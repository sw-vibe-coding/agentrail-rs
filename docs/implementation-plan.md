# Implementation Plan

This document replaces the Phase 0-4 plan from `continue-from-bootstrap.md`
with a revised plan reflecting the two-layer architecture.

## Completed

### Phase 0: Walking Skeleton (done)

- 8 CLI commands: init, status, next, begin, complete, plan, history, abort
- File-based persistence (TOML + JSON + JSONL)
- Saga lifecycle: create, advance, complete
- Step role typing (Meta, Production, Deterministic, Validation, Legacy)
- 24 integration tests
- Pre-commit quality gates passing

## Phase 1: ICRL Core Loop

**Goal**: make `agentrail next` inject past successful trajectories into its
output. This is the single highest-ROI change -- the ICRL payoff with
minimal refactoring.

### 1a. Add task_type to steps

Add an optional `task_type: Option<String>` field to `StepConfig`. This is
the key that links a step to its skill and experience libraries.

- Update `StepConfig` in `agentrail-core`
- Add `--task-type <type>` flag to `complete` command (for setting task_type
  on the next step)
- Update `create_step()` to accept task_type
- Add tests

### 1b. Wire trajectory retrieval into `next`

When `agentrail next` encounters a step with a non-null `task_type`:

1. Call `retrieve_successes()` for that task type
2. Format results as structured examples in the output
3. Include between the prompt section and the "when done" section

Format:

```
=== PAST SUCCESSES (tts) ===
[run_005] action=gradio_client, result=ok, reward=+1
[run_003] action=gradio_client, result=ok, reward=+1
```

- Update `next` command
- Add tests with pre-populated trajectory data

### 1c. Record experiences on complete

When `agentrail complete` finishes a step that has a `task_type`:

1. Prompt or accept experience metadata (actions taken, result, reward)
2. Save as a trajectory record
3. Use existing `save_trajectory()` infrastructure

- Add `--reward <-1|0|1>` and `--actions <description>` flags to `complete`
- Update tests

**Estimated scope**: ~200 lines of code changes, ~10 new tests.

## Phase 2: Dual Memory (Skills + Experiences)

**Goal**: implement the XSkill dual-memory pattern with structured skill
documents and enriched experience records.

### 2a. Skill type and storage

- Add `Skill` type to `agentrail-core` (procedure, success_patterns,
  common_failures, output_contract)
- Add `skills/` storage in `agentrail-store` (load/save TOML per task_type)
- Skills can be seeded manually or generated by distill

### 2b. Experience type (enriched trajectory)

- Add `Experience` type extending `Trajectory` with trigger conditions,
  structured actions list, failure_mode, notes
- Add `experiences/` storage alongside existing `trajectories/`
- Backward-compatible: old trajectories readable as minimal experiences

### 2c. Skill injection into `next`

- When a skill doc exists for the step's task_type, inject the full
  procedure, success patterns, and failure modes into `next` output
- Skill content appears before experience examples

### 2d. Distill command

- `agentrail distill <task_type>`: analyze experiences, generate/update
  skill document
- Groups experiences by success/failure
- Extracts common action patterns and failure modes
- Writes versioned skill TOML

### 2e. Update tests

- Skill round-trip tests
- Experience round-trip tests
- Distill logic tests
- Integration test: complete with experience -> distill -> next shows skill

**Estimated scope**: ~500 lines new code, ~15 new tests.

## Phase 3: Domain Repo Support

**Goal**: enable per-domain knowledge repos with executors, validators,
and curated skill/experience libraries.

### 3a. Domain registry

- Add `domains.toml` config to `.agentrail/`
- Domain discovery: read domain.toml manifests from registered paths
- Task-type-to-domain mapping

### 3b. Executor trait and shell executor

- Define `Executor` trait in `agentrail-core`
- Implement shell executor: pipes JobSpec params as JSON to stdin,
  reads result JSON from stdout
- Route `agentrail run-next` to the correct domain executor by kind

### 3c. Validator trait and shell validator

- Define `Validator` trait in `agentrail-core`
- Implement shell validator: same stdin/stdout JSON interface
- Wire into `complete` or a new `agentrail validate` command

### 3d. Merged retrieval

- Skill retrieval merges local + domain repo skills (local overrides)
- Experience retrieval merges local + domain repo experiences
- Domain repo experiences are read-only from Layer 1's perspective

### 3e. Contribute command

- `agentrail contribute <task_type> --to <domain>`: copy local experiences
  to domain repo for curation

### 3f. First domain repo

- Create `agentrail-domain-media` as a separate GitHub repo
- Seed with skills for TTS, ffmpeg-concat, video-composite, probe
- Include shell executors and validators
- Prove the full loop: domain skill + local experience -> next -> agent
  succeeds -> complete with reward -> distill updates skill

**Estimated scope**: ~800 lines new code in agentrail-rs, new repo for
domain-media, ~20 new tests.

## Phase 4: Knowledge Graph Validation

**Goal**: optional structured reward signals from task-type tool chains.

### 4a. Graph definition format

- TOML schema for directed graphs (edges with from/to/required)
- Load from domain repo `graphs/` directory

### 4b. Graph-based reward computation

- Compare experience action sequence against expected graph
- Compute structured reward: which required edges were followed/skipped
- Replace bare i8 reward with `RewardDetail` type

### 4c. Graph-aware distillation

- Distill command uses graph structure to identify systematic deviations
- Failure modes auto-categorized by which graph edge was violated

**Estimated scope**: ~400 lines, ~10 tests.

## Phase 5: Hybrid Orchestrator Loop

**Goal**: auto-advance through deterministic steps, escalate to agent only
on failure or for semantic work.

- Meta -> Production -> Deterministic -> Validation cycle
- `agentrail run-loop`: execute all deterministic steps in sequence,
  pause at production/meta steps for agent
- Resume after interruption
- Record experience on every step completion

**Estimated scope**: ~300 lines, ~10 tests.

## Briefing system — deferred items

The briefing / clearinghouse system (`agentrail instructions {status, apply,
diff, show, list}`, embedded `agent-instructions/` fragments, markered
regions in CLAUDE.md / AGENTS.md, freshness check on `agentrail next`,
`auto_apply`, and per-repo `exclude`) is shipped. The list below tracks
deferred extensions — none are required to use the system today, but each
addresses a specific friction point users have already raised.

### Bundled alt profiles

**Idea:** ship one or more named profiles alongside `default`, e.g.
`no-push` (default minus `global/push-discipline.md`), `minimal` (just
`baseline.md` + `session-protocol.md`).

**Status:** not done, and the use cases that motivated this turned out
to be covered by simpler mechanisms:

- *Per-user push privilege* (some users on a project can push, others
  can't): solved by `exclude = ["global/push-discipline.md"]` in
  `instruction-profile.toml` plus site-specific handoff content as
  local sections of CLAUDE.md outside the briefing markers. No new
  profile needed; agentrail-rs stays opinion-free about your specific
  release-engineering flow.
- *Wanting a leaner briefing*: `exclude` covers it per-repo.

Add a named profile only if the same exclude pattern turns up
unmodified in 3+ repos AND the additional content is also generic
(not site-specific). Hasn't happened yet.

**Effort:** small if/when justified. Add a TOML file under
`agent-instructions/profiles/` and a `(name, content)` entry in
`EMBEDDED_PROFILES`.

### Per-target rendering shapes

**Idea:** let `instruction-profile.toml` configure different content for
CLAUDE.md vs AGENTS.md (e.g. AGENTS.md gets a thin pointer + briefing
block, CLAUDE.md gets the full block alongside repo-local rules). Today
both targets receive the identical rendered body.

**Status:** not done. `agentrail setup` now creates a thin AGENTS.md
pointer and a fuller CLAUDE.md as separate stubs, so the local content
already differs — but the briefing block stamped into each is
byte-identical. A `target_template` mechanism could let the profile
specify which fragments go to which target.

**Effort:** medium. Either a per-target include list, or a render-time
filter keyed on the target file name.

### `--exclude` CLI flag on `apply`

**Idea:** support `agentrail instructions apply --exclude
global/push-discipline.md` for ad-hoc tweaks without editing
`instruction-profile.toml`.

**Status:** not done. Config-file persistence is the primary surface
because excludes need to survive across re-applies; a CLI flag would
have to be re-passed on every apply, which is fragile.

**Effort:** small. Add an optional `Vec<String>` flag to the apply
subcommand and pass through to `apply_to_file` / `apply`.

### Conflict detection

**Idea:** warn when project-specific text outside the markered region
contradicts a directive inside it (e.g. CLAUDE.md says "use `git add
-A`" while `global/git-hygiene.md` says the opposite).

**Status:** not done. Hard to detect without semantic understanding of
the prose. A possible weak heuristic: lint for known anti-patterns
(`git add -A`, `--no-verify`, etc.) against the local sections.

**Effort:** medium-large. Heuristic-based at first, with false-positive
risk.

### `instructions profile init` scaffolder

**Idea:** `agentrail instructions profile init [--commented]` writes a
fully-commented `.agentrail/instruction-profile.toml` template showing
all four fields (profile, targets, auto_apply, exclude) with example
values commented out. Newcomers see the entire surface in one place
instead of discovering it through `--help`.

**Status:** not done. Today's `add-exclude` / `set-targets` / etc.
will create the file lazily on first write, but the file only contains
the fields the user has touched. A scaffolder would be friendlier as a
discovery aid.

**Effort:** small. ~30 lines: a constant template string + a new
`ProfileEdit::Init { force: bool }` variant + a clap subcommand. Refuse
to overwrite without `--force`.

### Narrow-terminal schema rendering

**Idea:** the inline schema in `agentrail instructions --help` puts
field comments on the same line as values. On terminals narrower than
~80 cols those wrap awkwardly.

**Status:** deferred. Splitting `value` and `# comment` onto separate
lines fixes narrow terminals but worsens readability at standard
widths (doubles the vertical space). Only revisit if multiple users
hit narrow-terminal mush.

### Saga-aware `instructions show` (DONE)

**Idea:** make `agentrail instructions show` print the body that `apply`
would render in the current repo (honoring `profile` + `exclude`),
rather than always printing the embedded default.

**Status:** done. `show` reads `.agentrail/instruction-profile.toml`
when available and falls back to the embedded default with no excludes
when run outside a saga.

## Migration from Original Plan

| Original phase | New phase | Changes |
|---------------|-----------|---------|
| Phase 0 (walking skeleton) | Phase 0 | Done, unchanged |
| Phase 1 (deterministic execution) | Phase 3b-3c | Moved later; executors live in domain repos |
| Phase 2 (handoff packets) | Phase 2a-2c | Evolved into dual memory; HandoffPacket becomes Skill |
| Phase 3 (trajectory retrieval) | Phase 1b | Accelerated; highest ROI |
| Phase 4 (orchestrator loop) | Phase 5 | Unchanged, still last |
| (new) | Phase 1a, 1c | task_type field, experience recording |
| (new) | Phase 2d | Distill command (XSkill accumulation) |
| (new) | Phase 3 | Domain repo support |
| (new) | Phase 4 | KG validation |
